# LearningOps Suite synthetic demo fixtures

These fixtures support two approved LearningOps Suite demos:

1. `training-from-scratch/` — source signal → needs analysis → module design →
   rollout roster → ComplyOS evidence path.
2. `fix-messy-training-ops/` — messy CSV/LMS export → normalization → gaps →
   learner drafts → manager brief → ComplyOS evidence packet path.

All records are synthetic. Do not replace these fixtures with real client,
employee, student, school, or employer data.

